import main from '../styles/main.module.scss';

export default function Home() {
  return (
    <main>
      <h2 className={`${main.customText} ${main.textType1}`}>
        DJA NEXT TEMPLATE
      </h2>
      <p className={`${main.customText} ${main.textType2}`}>eiwoghwogehowh</p>
    </main>
  );
}
